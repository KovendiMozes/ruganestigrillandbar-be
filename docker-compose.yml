name: rugonfalva

services:
  postgres:
    image: postgres:16-alpine
    container_name: rugonfalva-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-rugonfalva}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rugonfalva}
      POSTGRES_DB: ${POSTGRES_DB:-rugonfalva}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-rugonfalva} -d ${POSTGRES_DB:-rugonfalva}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
